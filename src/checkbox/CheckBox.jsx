import React from 'react';

const CheckBox = (props) => {
    const { a } = props;
    return (
        <div className="aladdin-checkbox">
            Hello CheckBox!
            {a}
        </div>
    );
};

export default CheckBox;
